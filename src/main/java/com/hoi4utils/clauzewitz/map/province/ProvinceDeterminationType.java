package main.java.com.hoi4utils.clauzewitz.map.province;

// todo make this a class? so other types can be loaded with basic details
public enum ProvinceDeterminationType {
    DISTANCE,
    DISTANCE_MULTITHREADED,
    DISTANCE_GPU
}
