package net.updater



object Updater {
  def main(args: Array[String]): Unit = {
    update()
  }

  def update(): Unit = {
    println("Updating...")
    // download the latest version from github releases
    

    // unzip it to the appDir
    // delete the old version
    // rename the new version to the old version
//    // run the new version
//    val targetJarPath = "/HOIIVUtils/target/HOIIVUtils.jar"
//    Runtime.getRuntime.exec(s"java -jar ${targetJarPath}")
  }
}